# Core app
streamlit>=1.31,<2

# Computer vision (AruCo lives in "contrib"); headless is ideal for Streamlit servers
opencv-contrib-python-headless>=4.7,<5

# Image + array utils
Pillow>=10,<12
numpy>=1.26,<3

# Barcode decoding (ZXing C++ Python bindings)
zxing-cpp>=2.2,<3

# API calls (Open Food Facts lookups, optional OG image scraping)
requests>=2.31,<3
